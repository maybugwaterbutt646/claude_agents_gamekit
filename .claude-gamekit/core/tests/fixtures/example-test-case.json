{
  "case_id": "case-example-clean-template-spec",
  "feature_id": "example-clean-template",
  "preconditions": [
    "Task is active.",
    "FeatureSpec exists for the example slice."
  ],
  "steps": [
    "Open the feature spec for the active slice.",
    "Check that acceptance criteria are explicit and testable.",
    "Confirm the expected verification artifact path is defined."
  ],
  "expected": [
    "The feature spec contains enough detail for QA case generation.",
    "The slice can proceed without relying on hidden conversation history."
  ],
  "evidence_required": [
    "feature spec reference",
    "verification artifact path"
  ],
  "automation_level": "static"
}
